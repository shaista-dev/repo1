name: My first workflow
on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - name: Welcone ms
              run: echo "My first firhub actions job"
            - name: List files
              run: ls
            - name: Make script executable
              run: chmod +x hello.sh
            - name: execute shell script
              run: ./hello.sh